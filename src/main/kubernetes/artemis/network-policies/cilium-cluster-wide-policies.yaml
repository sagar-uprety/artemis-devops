# apiVersion: cilium.io/v2
# kind: CiliumClusterwideNetworkPolicy
# metadata:
#   name: allow-non-privileged-ports
# spec:
#   description: "Allow egress to non-privileged ports for pods in non-system namespaces, implicitly denying privileged ports"
#   endpointSelector:
#     matchExpressions:
#       - {key: io.kubernetes.pod.namespace, operator: NotIn, values: ["kube-system"]}
#   egress:
#     - toEntities:
#         - world
#       toPorts:
#         - ports:
#             - port: "1024-65535"
#               protocol: TCP
#         - ports:
#             - port: "1024-65535"
#               protocol: UDP
#     # Allow DNS
#     - toEndpoints:
#         - matchLabels:
#             k8s-app: kube-dns
#       toPorts:
#         - ports:
#             - port: "53"
#               protocol: UDP
#             - port: "53"
#               protocol: TCP