apiVersion: v1
kind: Service
metadata:
  name: artemis-app-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    workload.user.cattle.io/workloadselector: deployment-artemis-artemis